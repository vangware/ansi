import { backgroundColor } from "./backgroundColor";
import { BACKGROUND_BRIGHT_BLUE } from "./codes";

/**
 * Takes a `source` string and adds the ANSI codes for **bright blue**
 * background.
 *
 * @category Background
 * @returns `source` string with **bright blue** background.
 */
export const backgroundBrightBlue = backgroundColor(BACKGROUND_BRIGHT_BLUE);
