pre-push:
    commands:
        clean:
            run: rimraf ./dist
        compile:
            run: tsc --project ./tsconfig.dist.json
        lint:
            run: eslint {src/**/*.ts,tests/**/*.js}
        test:
            run: c8 node ./tests
        document:
            run: typedoc
